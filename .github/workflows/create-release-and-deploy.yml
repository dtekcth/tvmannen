name: Create release and deploy

on:
  workflow_dispatch:
    inputs:
      update_type:
        required: true
        default: "patch"
        type: choice
        options:
          - patch
          - minor
          - major

jobs:
  create-release-and-deploy:
    uses: dtekcth/workflows/.github/workflows/create-release-and-deploy.yml@main
    permissions:
      contents: write
      packages: write
    with:
        update_type: ${{ inputs.update_type }}
